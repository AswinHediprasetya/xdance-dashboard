import { LessonsPageContent } from './LessonsPageContent';

export const metadata = { title: 'Lesson Analysis — X-Dance' };

export default function LessonsPage() {
  return <LessonsPageContent />;
}
